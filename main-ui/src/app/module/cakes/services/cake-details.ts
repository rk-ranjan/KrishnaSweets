import { AddCake } from '../../admin/models/add-cake';
import { AddImages } from '../../admin/models/add-images';

export class CakeDetails {
    public items: AddCake;
    public image: AddImages;
}
